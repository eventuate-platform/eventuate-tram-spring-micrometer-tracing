# Application name
spring.application.name=tracing-test

# Tracing configuration (management.tracing.enabled and propagation.type are set via @DynamicPropertySource)
management.tracing.sampling.probability=1.0

# Zipkin configuration (endpoint set dynamically in test)
management.zipkin.tracing.enabled=true
